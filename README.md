# gwt_StockWatcher
from the gwt tutorial demo of web app development in Java
built in Eclipse with Google Cloud plug-in and GWT Plug-in which automates the build process using Jetty as the default server.
